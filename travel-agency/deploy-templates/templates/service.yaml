apiVersion: v1
kind: Service
metadata:
  name: {{ include "travel-agency.fullname" . }}
  labels:
    {{- include "travel-agency.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "travel-agency.selectorLabels" . | nindent 4 }}
